import React from 'react';
import './Account.scss';

// turn into component (drop down when icon is clicked)
const Account = () => {
  return (
    <section>
      <img src="images/money.png" alt="Help" />
    </section>
  );
};

export default Account;